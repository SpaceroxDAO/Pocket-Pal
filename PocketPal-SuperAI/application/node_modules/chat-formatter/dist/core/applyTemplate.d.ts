import { Conversation } from '../types/conversation';
import { TemplateConfig } from '../types';
interface ApplyTemplateOptions {
    templateKey?: string;
    customTemplate?: TemplateConfig;
    addGenerationPrompt?: boolean;
    isBeginningOfSequence?: boolean;
    isEndOfSequence?: boolean;
}
declare const applyTemplate: (conversation: Conversation | Conversation[], options?: ApplyTemplateOptions) => string | string[];
export default applyTemplate;
