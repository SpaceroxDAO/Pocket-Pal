"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.setupNunjucksEnvironment = void 0;
const nunjucks_1 = __importDefault(require("nunjucks"));
const utils_1 = require("./utils");
function setupNunjucksEnvironment() {
    const env = new nunjucks_1.default.Environment(null, { autoescape: false });
    env.addGlobal('raise_exception', (message) => {
        throw new Error(`${message}`);
    });
    env.addGlobal('strftime_now', (format) => {
        const now = new Date();
        return (0, utils_1.strftime)(now, format);
    });
    return env;
}
exports.setupNunjucksEnvironment = setupNunjucksEnvironment;
